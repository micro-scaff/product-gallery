[app]
port = 18080

[database]
host = 127.0.0.1
port = 3306
username = root
password = admin
database = product_gallery

[flow_talk]
; 临时联调使用 demo provider。Flow Talk Server 当前只内置 demo provider。
provider = demo
base_url = http://127.0.0.1:8080
; 临时模拟一个 C 端咨询用户，用来快速创建 Flow Talk 单聊会话。
demo_peer_access_token = product-gallery-demo-client
