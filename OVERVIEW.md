# Product Gallery 产品需求与范围说明

> 文档状态：需求基线（待评审）
>
> 适用范围：管理后台、C 端商品展示、用户体系、Flow Talk 聊天集成
>
> 本文用于统一产品、设计、前端、后端和测试对首期范围的理解；未明确的技术选型不在本文中预设。

## 1. 项目概述

Product Gallery 是一个面向商品展示与咨询场景的 Web 系统，由两个业务端和一个外部聊天系统组成：

- **管理后台**：供超级管理员和普通管理员维护人员、商品及聊天配置，并通过管理员聊天界面接收和回复用户消息。
- **C 端**：供游客或注册用户浏览商品、查看详情、管理个人资料并发起咨询。
- **Flow Talk**：承载会话、消息和 WebSocket 实时通信能力，本系统负责身份映射、商品上下文和聊天路由。

首期目标是完成“管理员发布商品 → 用户浏览商品 → 用户围绕商品发起咨询”的业务闭环。

### 1.1 首期范围

- 两级后台权限与管理员管理。
- 用户管理和用户个人中心。
- 商品的创建、编辑、上下架、查看和归属管理。
- Markdown 商品详情编辑，以及 PDF、Word 文档转 Markdown。
- PC 与移动端自适应的商品列表和详情页。
- 游客设备身份、手机号密码登录及登录后的身份合并。
- 商品级聊天开关、全局聊天开关、接待方配置及 Flow Talk 接入。
- 管理员聊天界面，支持管理员在 PC 和手机浏览器中查看并回复消息。

## 2. 术语与身份

| 术语 | 定义 |
| --- | --- |
| 超级管理员 | 系统最高权限角色，可查看和管理全局数据。 |
| 普通管理员 | 商品运营或商户角色，只能管理授权范围内的数据。 |
| 用户 | 已有业务账号并完成登录的 C 端访问者。 |
| 游客 | 未登录的 C 端访问者，以设备 ID 作为临时身份。 |
| 商品负责人 | 商品默认归属的普通管理员；没有负责人时由超级管理员接待咨询。 |
| 设备 ID | C 端基于设备信息生成的匿名标识，用于关联游客行为与聊天记录。 |
| 业务用户 ID | Product Gallery 内部稳定且唯一的用户标识。 |
| Flow Talk 用户 ID | 业务身份同步到 Flow Talk 后得到的内部用户标识。 |

## 3. 角色与权限

系统首期采用“超级管理员 + 普通管理员”两级后台权限。

| 能力 | 超级管理员 | 普通管理员 |
| --- | :---: | :---: |
| 登录管理后台 | ✓ | ✓ |
| 查看所有商品 | ✓ | — |
| 查看本人负责的商品 | ✓ | ✓ |
| 新建商品 | ✓ | ✓ |
| 编辑、上下架、删除本人负责的商品 | ✓ | ✓ |
| 编辑、上下架、删除其他管理员的商品 | ✓ | — |
| 指定或变更商品负责人 | ✓ | — |
| 查看、创建、编辑、禁用普通管理员 | ✓ | — |
| 查看和管理 C 端用户 | ✓ | ✓ |
| 查看全部聊天会话 | ✓ | — |
| 查看本人接待的聊天会话 | ✓ | ✓ |
| 在 PC 或手机上回复权限范围内的消息 | ✓ | ✓ |
| 配置系统级聊天开关 | ✓ | — |
| 配置商品级聊天开关和接待方 | ✓ | 仅本人商品 |

### 3.1 超级管理员

- 首期仅保留一个超级管理员，不提供在管理后台新建超级管理员的入口。
- 默认账号为 `admin`，默认密码为 `product-gallery`。
- 超级管理员账号由服务端配置文件控制是否启用，创建多个 超级管理员。
- 普通管理员不能查看、编辑、禁用或删除超级管理员。

### 3.2 普通管理员

- 由超级管理员创建、编辑、禁用或重置密码。
- 默认只能查看和管理自己负责的商品及其关联会话。
- 管理员被禁用后不得继续登录；已有登录会话应失效。
- 禁用管理员前，需要先处理其名下商品和未结束会话，可转交给其他管理员或超级管理员。

### 3.3 C 端用户管理

- 管理员可按用户 ID、手机号或状态查询用户。
- 支持查看用户基本资料、注册/创建时间、最后登录时间和账号状态。
- 支持禁用、启用及重置密码；禁用后用户不能登录或继续发送消息。
- 普通管理员不可以查看全部用户的完整手机号。

## 4. 商品管理

### 4.1 商品字段

| 字段 | 必填 | 说明 |
| --- | :---: | --- |
| 商品 ID | 系统生成 | 全局唯一，不因标题或负责人变更。 |
| 标题 | 是 | 列表与详情页的主标题。 |
| 简介 | 是 | 列表摘要和详情页概述。 |
| 价格 | 否 | 使用定点小数存储，禁止浮点数；币种首期默认为人民币。 |
| 封面图 | 否 | 用于商品列表和分享预览。 |
| 详情 | 是 | Markdown 原文，渲染为安全 HTML 后展示。 |
| 负责人 | 否 | 普通管理员；为空时聊天默认转给超级管理员。 |
| 状态 | 是 | 草稿、已上架、已下架。 |
| 聊天开关 | 是 | 继承系统配置、开启或关闭，具体规则见第 7 节。 |
| 创建人/更新时间 | 系统生成 | 用于审计、排序和问题追踪。 |

### 4.2 生命周期

1. 管理员创建商品时，商品默认为“草稿”。
2. 管理员填写字段并编辑或导入详情。
3. 必填字段校验通过后，商品可以上架。
4. 只有“已上架”商品对 C 端可见。
5. 商品下架后不可从 C 端列表或详情页继续访问，但已有聊天和历史消息仍保留。
6. 删除建议采用软删除；存在历史会话的商品不应物理删除。

普通管理员新建的商品默认归属于本人。超级管理员可以新建无负责人商品，也可以变更任意商品负责人。

### 4.3 Markdown 详情编辑

- 编辑器至少提供 Markdown 输入、预览和保存能力。
- 支持常用标题、段落、列表、表格、代码块、链接和图片。
- 服务端保存 Markdown 原文；展示时必须过滤脚本、危险标签和危险 URL，防止 XSS。
- 自动保存、图片上传、历史版本和多人协作不作为首期强制要求。

### 4.4 PDF、Word 转 Markdown

- 支持上传 `.pdf`、`.doc` 和 `.docx` 文件；如转换组件不支持旧版 `.doc`，前端应明确提示用户先转为 `.docx`。
- 上传后由服务端执行解析和转换，成功结果自动回填到 Markdown 编辑器。
- 回填内容仍可人工修改，只有用户点击保存后才写入商品详情。
- 转换失败不得覆盖编辑器中已有内容，并应返回可理解的失败原因。
- 扫描版 PDF 需要 OCR 才能识别；OCR 是否纳入首期需另行确认。
- 应配置单文件大小、页数、解析超时和允许的 MIME 类型，并清理临时文件。
- 转换结果不保证完全保留复杂排版，表格、图片、公式和分页需要在验收样例中单独确认。

## 5. C 端商品展示

### 5.1 页面

- **商品列表页**：展示封面、标题、简介和价格，点击进入商品详情。
- **商品详情页**：展示完整商品信息、Markdown 渲染内容和聊天入口。
- **登录页/弹窗**：手机号、密码和图形验证码。
- **个人中心**：查看和修改头像、手机号及密码。

页面需要兼容桌面端和移动端常见视口，不能仅通过缩放桌面布局适配手机。

### 5.2 可见性规则

- 游客和已登录用户均可浏览已上架商品。
- 草稿、已下架和已删除商品不出现在 C 端。
- 通过旧链接访问不可见商品时返回统一的“商品不存在或已下架”页面，不泄露后台状态。
- 价格展示格式、是否显示人民币符号及是否允许零价格需产品确认。

## 6. 游客、登录与个人中心

### 6.1 游客身份

- 用户首次访问 C 端时，前端使用 FingerprintJS 生成设备 ID。
- 设备 ID 只能作为匿名关联线索，不能作为可靠的身份认证凭证。
- 设备 ID 应持久化在本地，并随允许匿名访问的业务请求提交给后端。
- 浏览器隐私策略、无痕模式、清理存储或设备信息变化都可能导致 ID 变化，系统不得假设它永久不变。
- 后端应保存首次访问时间、最后访问时间及关联的业务用户 ID；不保存生成指纹所需的原始设备特征。

### 6.2 登录流程

1. 游客输入手机号和密码。
2. 手机号格式有效后展示或加载图形验证码；登录请求必须同时提交验证码答案和验证码凭证。
3. 服务端校验验证码、账号状态和密码。
4. 登录成功后，前端将当前设备 ID 与登录凭证一起提交给后端。
5. 后端将该设备 ID 关联至业务用户，并合并允许迁移的游客数据。
6. 前端刷新登录态，后续操作使用业务用户身份。

### 6.3 游客数据合并

- 同一设备 ID 可以关联一个已登录用户；用户再次登录时更新最后使用时间。
- 同一用户可以关联多个设备 ID。
- 需要合并的数据至少包括游客聊天身份和未结束的商品咨询上下文。
- 合并过程必须幂等，重复提交同一设备 ID 不得产生重复用户或重复会话。
- 如果设备 ID 已关联其他账号，不得静默转移，需记录安全日志并按明确策略处理。

### 6.4 个人中心

- 用户可以查看头像、手机号和账号状态。
- 支持上传或更换头像，并限制文件类型和大小。
- 修改密码时需要验证当前密码；管理员重置密码的场景除外。
- 修改密码或手机号后是否使其他设备登录态失效。

## 7. 聊天系统

聊天能力由 [Flow Talk Server](https://github.com/micro-scaff/flow-talk-server) 提供，接入实现以其[外部身份系统完整接入指南](https://github.com/micro-scaff/flow-talk-server/blob/main/docs/%E5%A4%96%E9%83%A8%E7%B3%BB%E7%BB%9F%E5%AE%8C%E6%95%B4%E6%8E%A5%E5%85%A5%E6%8C%87%E5%8D%97.md)为准。

### 7.1 聊天开关

系统包含两级聊天开关，判断顺序如下：

1. 系统级聊天关闭时，所有商品均不显示聊天入口。
2. 系统级聊天开启时，再判断商品级设置。
3. 商品级为“关闭”时不显示聊天入口。
4. 商品级为“开启”或“继承系统配置”时显示聊天入口。

为避免配置含义不清，商品级字段采用“继承 / 开启 / 关闭”三态，而不是简单布尔值。

### 7.2 接待方路由

用户从商品详情发起聊天时，接待方按以下优先级确定：

1. 商品显式指定的普通管理员。
2. 超级管理员兜底。

普通管理员只能访问自己上传的商品会话，删除了管理员后，自动转给超级管理员。超级管理员可查看全部会话，并可变更接待方。

### 7.3 管理员聊天界面

管理后台提供独立的聊天工作台，采用响应式 Web 页面，同时支持 PC 和手机浏览器，无需单独开发手机 App。

#### 7.3.1 界面与功能

- 会话列表展示咨询用户、关联商品、最后一条消息、最后消息时间、未读数量和会话状态。
- 超级管理员可查看全部会话；普通管理员仅能查看自己上传商品对应的会话。
- 管理员进入会话后，可查看历史消息、用户基本信息和关联商品信息。
- 管理员可以发送文本消息，并实时接收 C 端用户的新消息；其他消息类型以 Flow Talk 实际支持能力为准。
- 消息发送过程需要显示发送中、发送成功和发送失败状态；发送失败时允许重新发送。
- 新消息到达后更新会话排序和未读数量，管理员打开会话后将消息标记为已读。
- 切换会话、刷新页面或更换设备后，历史消息仍从 Flow Talk 加载，不依赖浏览器本地记录。
- 管理员被删除后，其会话自动转给超级管理员，原管理员不能继续查看或回复。

#### 7.3.2 手机端交互

- 手机端默认展示会话列表，点击会话后进入消息详情，并可返回会话列表。
- 消息输入框固定在可操作区域，软键盘弹出时不能遮挡输入内容或发送按钮。
- 页面适配手机安全区域、竖屏宽度和触控操作，主要按钮具备足够的点击范围。
- 手机端具备与 PC 端一致的查看历史消息、接收新消息和回复消息能力。

#### 7.3.3 验收要求

- 管理员能够分别在 PC 和手机浏览器中打开聊天工作台并正常切换会话。
- C 端发送新消息后，管理员界面无需手动刷新即可收到消息和未读提示。
- 管理员从手机端回复后，C 端能够实时收到消息，且双方刷新页面后仍能查看历史记录。
- 超级管理员和普通管理员只能查看、回复各自权限范围内的会话。

### 7.4 会话业务上下文

每个从商品页创建的会话至少保存以下业务侧关联：

- Product Gallery 会话关联 ID。
- 商品 ID 和发起时的商品标题快照。
- 发起方类型（游客或用户）及对应业务身份 ID。
- 当前接待管理员 ID。
- Flow Talk 会话 ID。
- 创建时间、最后消息时间和会话状态。

同一用户针对同一商品重复点击咨询时，复用之前的会话。

### 7.5 Flow Talk 身份与 Token 边界

Product Gallery 是业务身份源，Flow Talk 只负责 IM 身份和消息能力：

1. 用户或管理员先在 Product Gallery 完成登录。
2. Product Gallery 服务端签发短期外部身份 Token，或提供可被 Flow Talk 服务端验证的 Token。
3. 客户端调用 Flow Talk `POST /api/auth/external`，提交固定的 `provider` 和外部 Token。
4. Flow Talk 验证外部 Token、同步内部用户并返回 Flow Talk JWT。
5. 后续聊天 HTTP API 和 WebSocket 只使用 Flow Talk JWT。

必须遵守以下约束：

- 使用稳定且不可变的业务用户 ID 作为外部身份，不能使用昵称、手机号或设备名称代替。
- 游客 ID 和登录用户 ID 使用不同命名空间，避免冲突。
- Product Gallery 密码不得提交给 Flow Talk。
- 外部 Token 仅用于换取 Flow Talk JWT，不写入 Flow Talk 数据库，不放入 URL 或日志。
- Flow Talk JWT 不作为 Product Gallery 业务接口的登录凭证。
- 用户、管理员被禁用后，必须阻止继续换票；已有聊天登录态的失效策略需要在联调时确认。

### 7.6 异常与降级

- Flow Talk 不可用时，商品浏览仍应可用，聊天入口显示“暂时无法连接”。
- 创建会话或发送消息失败时提供重试，不得显示已发送但实际丢失的消息。
- 商品下架、负责人变更不删除历史消息。
- 聊天开关关闭后，不允许创建新会话；已有会话是否还能继续发送消息需产品确认，建议系统级关闭时全部只读，商品级关闭时保留历史但禁止新消息。

## 8. 核心数据对象

以下为概念模型，不限定数据库类型或具体表结构。

| 对象 | 关键字段 |
| --- | --- |
| Admin | id、role、username、password、status、created_at、updated_at |
| User | id、phone、password、avatar_url、status、created_at、last_login_at |
| VisitorDevice | id、device_fingerprint、user_id、first_seen_at、last_seen_at |
| Product | id、title、summary、price、cover_url、detail_md、owner_admin_id、status、chat_policy、created_by、timestamps |
| ChatAssignment | product_id、receiver_admin_id、enabled、updated_by、updated_at |
| ChatBinding | id、product_id、visitor_device_id/user_id、receiver_admin_id、flow_talk_conversation_id、status、timestamps |
| AuditLog | actor、action、target_type、target_id、result、created_at、request metadata |

业务数据应通过内部 ID 建立关系；手机号、标题和用户名等可变字段只用于展示或搜索。

## 9. 安全与隐私要求

### 9.1 密码存储要求

账号密码使用明文存储，这是已确认的业务要求：

- 超级管理员、普通管理员和 C 端用户的密码均以原始明文写入数据库。
- 密码不加密、不哈希、不加盐，也不进行其他形式的转换。
- 创建账号、修改密码和重置密码时，数据库直接保存提交的新密码。
- 登录时使用用户提交的密码与数据库中的明文密码进行匹配。

## 10. 非功能要求

- **兼容性**：支持主流现代浏览器最近两个稳定版本；C 端和管理员聊天界面均覆盖常见手机与桌面宽度。
- **可用性**：聊天服务故障不影响商品浏览和后台商品维护。
- **一致性**：创建会话、游客身份合并和外部身份同步必须支持幂等处理。
- **可观测性**：记录请求 ID、结构化错误和关键依赖耗时；监控登录失败、文档转换失败、Flow Talk 换票失败和 WebSocket 连接异常。
- **性能基线**：具体并发量、数据量和响应时间指标待部署规模明确后补充，不能在缺少容量假设时承诺数值。
- **备份恢复**：用户、商品、关系和聊天绑定数据需要备份；Flow Talk 消息数据按其部署方案单独备份。
